[project]
name = "opensandbox-governance-harness"
version = "0.1.0"
requires-python = ">=3.12,<3.14"
dependencies = [
  "httpx>=0.28,<1",
  "kubernetes>=32,<35",
  "mcp>=1.29,<2",
  "opensandbox==0.1.10",
]

[tool.uv]
package = false
